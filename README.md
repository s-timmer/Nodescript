
  # NodeScript

  This is a code bundle for NodeScript. The original project is available at https://www.figma.com/design/lS8dICL3q6WNupgez4cFLX/NodeScript.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  